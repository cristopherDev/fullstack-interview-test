const URL_GIT = "https://api.github.com";
const USER = "cristopherDev";
const REPO = "fake-repo"

module.exports = {
    URL_REPO: `${URL_GIT}/repos/${USER}/${REPO}`,
    URL_BRANCHES: `${URL_GIT}/repos/${USER}/${REPO}/branches`
}
